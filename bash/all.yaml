apiVersion: apps/v1
kind: Deployment
metadata:
  name: bash
spec:
  selector:
    matchLabels:
      app: bash
  replicas: 1
  template:
    metadata:
      labels:
        app: bash
    spec:
      serviceAccountName: default
      automountServiceAccountToken: true
      containers:
      - name: bash
        image: bash
        imagePullPolicy: IfNotPresent
